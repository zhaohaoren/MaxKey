<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snowx.iam.persistence.mapper.AccountsMapper">

    <sql id="where_statement">
        <if test="id != null and id != ''">
            and id = #{id}
        </if>
        <if test="appId != null and appId != ''">
            and appid = #{appId}
        </if>
        <if test="username != null and username != ''">
            and username = #{username}
        </if>
        <if test="relatedUsername != null and relatedUsername != ''">
            and relatedusername = #{relatedUsername}
        </if>
    </sql>


    <select id="queryPageResults" parameterType="Accounts" resultType="Accounts">
        select
        *
        from
        mxk_accounts
        where instid = #{instId}
        <include refid="where_statement"/>
    </select>

    <select id="queryByAppIdAndDate" parameterType="Accounts" resultType="Accounts">
        select *
        from mxk_accounts
        where appid = #{appId}
        <if test="modifiedDate != null">
            and modifieddate &gt;= #{modifiedDate}
        </if>
        <if test="instId != null and instId != ''">
            and instid = #{instId}
        </if>
        order by modifieddate
    </select>

    <select id="queryByAppIdAndAccount" resultType="Accounts">
        select *
        from mxk_accounts
        where appid = #{appId}
          and relatedusername = #{relatedUsername}
    </select>

    <update id="updateStatus" parameterType="Accounts">
        update mxk_accounts
        set status = #{status},
            modifieddate = current_timestamp
        where id = #{id}
          and instid = #{instId}
    </update>

</mapper>
